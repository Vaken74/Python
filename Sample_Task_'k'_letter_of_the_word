# Sample_Task_'k'_letter_of_the_word
# The input to the program is a natural number 'n' and 'n' lines followed by a number 'k'. Write a program that outputs 'k' letter from the entered strings on one line without spaces.

n, y = int(input()), 0
l = []
for i in range(n):
    i = input()
    l.append(i)
k = int(input())
ll = []
for j in l:
    if len(j) > k - 1:
        ll.append(j[k - 1])
print(''.join(ll))
